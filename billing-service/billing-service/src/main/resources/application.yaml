server:
  port: 8083

spring:
  application:
    name: billing-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html